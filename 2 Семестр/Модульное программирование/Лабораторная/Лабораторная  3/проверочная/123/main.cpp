#include <iostream>
#include <clocale >


using namespace std;
struct structura {double ukr;
                    double vel;
                    double es;};
structura man ( reestr[i])
{   setlocale (LC_ALL, "rus");
    int mat=20;
    double x,y;
    while (!(cin>>y))
    {   cin.clear();
        cin.ignore();
        cout<< "Не корректное значение\n"; };


structura reestr[2];
reestr[0]={1,2,3};
reestr[0]={2,2,3};
}
